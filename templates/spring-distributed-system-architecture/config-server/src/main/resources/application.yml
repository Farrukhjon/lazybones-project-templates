server:
    port: 8888

    management:
      context-path: /admin

eureka:
    instance:
       leaseRenewalIntervalInSeconds: 10
       statusPageUrlPath: /admin/info
       healthCheckUrlPath: /admin/health

spring:
    cloud:
      config:
        server:
            git:
               uri: https://github.com/farrukhjon/config-repo
