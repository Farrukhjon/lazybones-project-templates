info:
  app:
    name: ${project.name}
    group: ${project.group}
    description: ${project.description}
    version: ${project.version}
spring:
  data:
    mongodb:
      database: testdb
      host: mongo
      port: 27017
logging:
  level:
    ROOT: INFO
    org:
      springframework:
        web: DEBUG
        data.mongodb: DEBUG
