spring:
  application:
    name: spring-cloud-rest-service
  profiles:
    active: testing
---
spring:
  profiles: testing
  cloud:
    config:
      discovery:
        enabled: false
eureka:
  client:
    enabled: false